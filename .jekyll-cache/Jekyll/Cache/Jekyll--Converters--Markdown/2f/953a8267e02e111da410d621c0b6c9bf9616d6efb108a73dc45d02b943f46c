I"�<h1 id="java-fx">Java FX</h1>

<h2 id="introducció">Introducció</h2>

<p>JavaFX li permet crear aplicacions Java amb una interfície d’usuari moderna accelerada per maquinari que és altament portàtil.</p>

<p>Per a obtindre informació sobre com executar aplicacions JavaFX en plataformes mòbils, consulte Introducció a <a href="https://docs.gluonhq.com/getting-started/">Gluon Mobile</a>.</p>

<p>Per a obtindre informació sobre el Suport a llarg termini (LTS) per a JavaFX 11, consulte les opcions de <a href="https://gluonhq.com/services/javafx-support/">Suport a llarg termini de JavaFX</a>.</p>

<p>JavaFX es basa en JDK i és un component independent. Hi ha 2 opcions diferents per a desenvolupar aplicacions JavaFX:</p>

<ul>
  <li>Utilitza el SDK de JavaFX</li>
  <li>Utilitza un sistema de compilació (per exemple, maven / gradle) per a descarregar els mòduls necessaris de Maven Central.</li>
</ul>

<p>Per a totes dues opcions, es requereix tindre una versió recent de JDK 14, o almenys JDK 11.</p>

<h2 id="installació-java">Instal·lació Java</h2>

<p>Descarrega un JDK apropiat per al sistema operatiu. L’últim JDK es pot descarregar des del lloc web oficial de <a href="http://jdk.java.net/14">OpenJDK</a>.</p>

<p>Pot ser el sistema ja tinga instal·lat el JDK.</p>

<p>Una vegada instal·lat, pot usar el comando java des de la seua línia de comando.</p>

<p>Comprove si està instal·lada la versió correcta de Java. A continuació es mostra el resultat per a OpenJDK 12:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java -version
versió de openjdk "12.0.2" 2019-07-16
OpenJDK Runtime Environment (compilació 12.0.2 + 10)
OpenJDK 64-Bit Server VM (compilació 12.0.2 + 10, manera mixta, ús compartit)
</code></pre></div></div>

<p>Ha d’establir la variable d’entorn JAVA_HOME en el directori d’instal·lació de JDK. Pot seguir aquesta guia per a configurar JAVA_HOME per a la seua plataforma.</p>

<blockquote>
  <p>Nota: Si el seu sistema té instal·lades diverses versions de JDK, ha d’assegurar-se que la variable d’entorn JAVA_HOME anotació a corregir JDK. JavaFX 14 necessita almenys JDK 11.</p>
</blockquote>

<h3 id="establir-la-variable-dentorn-java_home-en-el-directori-dinstallació-de-jdk">Establir la variable d’entorn JAVA_HOME en el directori d’instal·lació de JDK</h3>

<ol>
  <li>Modificar el PATH
    <ul>
      <li>La variable PATH, és una variable d’entorn que conté una llista ordenada de rutes on Linux cercarà executables quan execute un comandament. L’ús d’aquestes rutes, farà que no tingam que especificar una ruta absoluta al executar el comandament. Per la versió 11, utilitzarem la seguent acció:</li>
    </ul>
  </li>
</ol>

<p>Afegim la seguent linea al fitxer ~/.bashrc.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    export JAVA_HOME=/usr/lib/jvm/java-1.11.0-openjdk-amd64
</code></pre></div></div>

<p>Desem el fitxer i executem:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    source ~/.bashrc
</code></pre></div></div>

<p>Ara podem comprobar el valor de la variable JAVA_HOME</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    echo $JAVA_HOME
</code></pre></div></div>

<p>El resultat deu ser el següent o indicant la versió que tenim instal·lat:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/usr/lib/jvm/java-1.11.0-openjdk-amd64
</code></pre></div></div>

<h4 id="configuració-global-de-java_home">Configuració global de JAVA_HOME</h4>

<p>Per configurar JAVA_HOME per tots els usuaris, podem utilitzar els fitxers /etc/profile o /etc/environment (recomanat).</p>

<ul>
  <li>Utilitzant /etc/environment-&gt; Obrim el fitxer i li afegim:</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    JAVA_HOME=/usr/lib/jvm/java-1.11.0-openjdk-amd64
</code></pre></div></div>

<p>el fitxer /etc/environment el llig quan s’inicia la sessió.</p>

<ul>
  <li>Utilitzant /etc/profile -&gt; Obrim el fitxer i li afegim:</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    export JAVA_HOME=/usr/lib/jvm/java-1.11.0-openjdk-amd64
</code></pre></div></div>

<p>Executem</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    source /etc/profile
</code></pre></div></div>

<p>Ara podem comprobar el valor de la variable JAVA_HOME</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    echo $JAVA_HOME
</code></pre></div></div>

<p>El resultat deu ser el següent o indicant la versió que tenim instal·lat:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/usr/lib/jvm/java-1.11.0-openjdk-amd64
</code></pre></div></div>

<h2 id="installa-javafx-sdk">Instal·la JavaFX SDK</h2>

<p>SDK (Software Desktop Kit) és un conjunt d’eines bàsiques necessaries per compilar, executar e implementar aplicacions JavaFX. Es pot programar utilitzant un editor de text, interactuant directament amb les eines SDK. Però pot ser més comode utilitzar un IDE com NetBeans, Eclipse o Intellij.</p>

<p>per instalar JavaFx en linux:</p>

<p>obrim la linea de comandos i escrivim:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>apt install openjfx
</code></pre></div></div>

<p>per aconseguir el paquet. Que s’ha instal·lat en /usr/share/openjfx/lib.</p>

<p>En el terminal, executem la comanda:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>export PATH_TO_FX="/usr/share/openjfx/lib"
</code></pre></div></div>

<p>Pegant en la ruta absoluta al teu directori de descarregues.</p>

<p>Ara ja podem compilar i executar aplicacions JavaFX des de la linea de comandes.</p>

<p>Compila l’aplicació HelloFX.java amb:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>javac --module-path $PATH_TO_FX --add-modules javafx.controls HelloFX.java
</code></pre></div></div>

<p>Nota: Es requereixen mòduls addicionals per a una funcionalitat estesa. Per exemple, si la seua aplicació usa FXML, haurà d’agregar el mòdul javafx.fxml, com es mostra a continuació:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>javac --module-path $PATH_TO_FX --add-modules javafx.controls,javafx.fxml HelloFX.java
</code></pre></div></div>

<p>Assegure’s d’agregar els mòduls requerits, tenint en compte que les dependències transitives es resolen automàticament (per exemple, no és necessari agregar el mòdul javafx.graphics, ja que el mòdul javafx.controls ho requereix transitivament).</p>

<p>A continuació, execute l’aplicació usant:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java --module-path $PATH_TO_FX --add-modules javafx.controls HelloFX
</code></pre></div></div>

<p><a href="../javafx.html">back</a></p>
:ET